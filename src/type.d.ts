export interface IMaxtrixOptions {
  width: number;
  height: number;
  class: string;
  onColor: string;
  offColor: string;
  bgColor: string;
}

export interface IPixelOptions {
  color: string;
  onColor: string;
  offColor: string;
  bgColor: string;
}
